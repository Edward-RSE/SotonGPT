apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres
  namespace: sotongpt
  labels:
    app: postgres
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 64Gi
