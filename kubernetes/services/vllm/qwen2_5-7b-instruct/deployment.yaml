apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-qwen2-5-7b-instruct-deployment
  namespace: sotongpt
  labels:
    app: vllm-qwen2-5-7b-instruct-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-qwen2-5-7b-instruct-deployment
  template:
    metadata:
      labels:
        app: vllm-qwen2-5-7b-instruct-deployment
    spec:
      volumes:
        - name: cache-volume
          persistentVolumeClaim:
            claimName: qwen2-5-7b-instruct-pvc
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 8Gi
      containers:
        - name: vllm
          image: vllm/vllm-openai:v0.14.1
          args:
            - --model
            - Qwen/Qwen2.5-7B-Instruct-AWQ
            - --max-model-len
            - "65536"
            - --gpu-memory-utilization
            - "0.90"
            - --dtype
            - auto
            - --tensor-parallel-size
            - "1"
            - --max-num-batched-tokens
            - "8192"
            - --max-num-seqs
            - "96"
            - --enable-chunked-prefill
            - --port
            - "8000"
            - --host
            - 0.0.0.0
          ports:
            - containerPort: 8000
          env:
            - name: VLLM_LOGGING_LEVEL
              value: INFO
            - name: VLLM_ALLOW_LONG_MAX_MODEL_LEN
              value: "1"
          resources:
            limits:
              nvidia.com/gpu: "1"
              cpu: "8"
              memory: 64Gi
            requests:
              nvidia.com/gpu: "1"
              cpu: "4"
              memory: 24Gi
          volumeMounts:
            - name: cache-volume
              mountPath: /root/.cache/huggingface
            - name: shm
              mountPath: /dev/shm
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 5
            failureThreshold: 3
          # let's the container have 15 minutes to launch before k8s tries to
          # say that it's broken/not launching
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 5
            failureThreshold: 180
