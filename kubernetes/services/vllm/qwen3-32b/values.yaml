
# ---------------------------------------------------------------------------
# REQUIRED — must be provided at install time, no defaults
# ---------------------------------------------------------------------------
model:
  # Short name used to name the related pods, services and etc., e.g. "llama3-70b"
  name: qwen3-32b
  # HuggingFace model ID, e.g. "Qwen3/Qwen3-32B"
  id: abhishekchohan/Qwen3-32B-AWQ
  # Path to the directory, on the host, where model weights will be kept
  hostPath: /home/ejp1v21/sotongpt-models/vllm/qwen3-32b

# ---------------------------------------------------------------------------
# OPTIONAL — sensible defaults, override only when needed
# ---------------------------------------------------------------------------
namespace: sotongpt
numReplicas: "1"

# Free-form vLLM CLI args — append anything the vLLM docs support.
# The model ID is always prepended automatically, do not repeat it here
vllmArgs:
  # Model identity
  - --served-model-name
  - qwen3-32b
  - --quantization
  - awq_marlin
  - --dtype
  - float16
  # Parallelism
  - --tensor-parallel-size
  - "2"
  - --kv-cache-dtype
  - auto
  # Memory
  - --gpu-memory-utilization
  - "0.90"
  # Context & batching
  # - --hf-overrides
  # - '{"max_position_embeddings":131072,"rope_scaling":{"rope_type":"yarn","factor":4.0,"original_max_position_embeddings":32768}}'
  # - --max-model-len
  # - "131072"
  - "--max-num-seqs"
  - "64"
  # Optimisations
  - --enable-chunked-prefill
  - --enable-prefix-caching
  # Reasoning & tools
  - --reasoning-parser
  - qwen3
  # Network
  - --host
  - 0.0.0.0
  - --port
  - "8000"


# Resource requirements for pods. These values are probably sensible for most
# small- to mid-sized models
resources:
  limits:
    gpu: "2"
    cpu: "12"
    memory: 32Gi
  requests:
    gpu: "2"
    cpu: "6"
    memory: 16Gi
  storage: 64Gi
