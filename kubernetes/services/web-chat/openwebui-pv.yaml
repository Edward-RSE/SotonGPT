apiVersion: v1
kind: PersistentVolume
metadata:
  name: openwebui-pv
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/openwebui-data
    type: DirectoryOrCreate