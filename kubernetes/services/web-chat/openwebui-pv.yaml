apiVersion: v1
kind: PersistentVolume
metadata:
  name: openwebui-pv
spec:
  storageClassName: manual
  capacity:
    storage: 64Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/openwebui-data
    type: DirectoryOrCreate
