apiVersion: v1
kind: Service
metadata:
  name: openwebui-service
  namespace: sotongpt
spec:
  type: ClusterIP
  selector:
    app: openwebui
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: 8080
