apiVersion: v1
kind: PersistentVolume
metadata:
  name: ollama-pv
spec:
  storageClassName: manual
  claimRef:
    name: ollama-pvc
    namespace: sotongpt
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /opt/sotongpt-data/ollama
    type: DirectoryOrCreate
