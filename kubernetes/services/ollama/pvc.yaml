apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama
  namespace: sotongpt
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
