apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openwebui
  namespace: sotongpt
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 32Gi
