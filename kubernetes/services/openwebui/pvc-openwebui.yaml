apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openwebui
  namespace: sotongpt
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 32Gi
  volumeName: openwebui
